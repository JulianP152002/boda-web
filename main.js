const form = document.querySelector('form_button')
form.addEventListener('click', () =>{

    alert ("gracias por hacernos saber de tu asistencia")
    .then(() => {window.location.reload()})
    

})